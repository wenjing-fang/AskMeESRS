# Demo with checkpoint saved

## Usage
Download the three folders and put in the same folder. Run the demo_withcheckpoint.py code, the application will load the combined text and the vector store from the checkpoint folder automatically. If you want to recreat the text and vector store, simply remove the files "/checkpoints/faiss_index" and "/checkpoints/all_text.txt", and the application will create new ones. 
